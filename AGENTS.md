# Project Rules & Instructions

- **Stable Login Implementation**: DO NOT change the current Firebase login implementation (using `signInWithPopup` and the `.firebaseapp.com` authDomain). This configuration is confirmed working on library computers and user devices. Avoid adding "Library Mode" toggles or redirect fallbacks unless explicitly requested again, as the current popup flow is the preferred "main login".
